#include <iostream>

using namespace std;

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    int x, y, z;
    cin >> x >> y >> z;

    cout << (x * y / 2) << '\n';
    cout << (x + y + z) << '\n';
    return 0;
}
